# Nhập 1 chuỗi → in số ký tự

# Đếm số từ trong chuỗi

# In chuỗi đảo ngược

# Kiểm tra chuỗi có chứa từ "python" không

str = input('Nhap str: ')
print(f'str length: {len(str)}')
print(f'str so tu: {len(str.split())}')
print(f'str dao nguoc: {str[::-1]}')
print(f'str check python: {'python' in str}')
